"""LedgerDrop extraction evaluation dataset (Stage 3, step 10).

A small, manually verified set of synthetic invoice PDFs plus the ground-truth
field values, used to measure extraction accuracy before choosing confidence
thresholds or a real provider (steps 11 and 13). Everything here is generated by
``generate_invoices.py`` - no third-party or personal data.
"""
